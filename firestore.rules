rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORAL: Reglas completamente abiertas para pruebas
    // ⚠️ ADVERTENCIA: Solo usar para desarrollo/testing
    // TODO: Restaurar reglas de seguridad después de la migración
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 